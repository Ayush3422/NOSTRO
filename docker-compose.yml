services:
  api:
    build: .
    ports: ["8000:8000"]
  ui:
    image: node:20-slim
    working_dir: /app
    volumes: ["./ui:/app"]
    command: sh -c "npm install && npm run dev"
    environment:
      NEXT_PUBLIC_API: http://api:8000
    ports: ["3000:3000"]
    depends_on: [api]
